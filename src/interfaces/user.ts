export interface PasswordData {
  oldPassword: string;
  newPassword: string;
}
export interface UserData {
  first_name: string;
  second_name: string;
  display_name: string;
  login: string;
  email: string;
  phone: string;
}